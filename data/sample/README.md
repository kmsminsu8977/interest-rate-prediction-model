# Sample Data

이 폴더의 `macro_rate_panel.csv`는 baseline 계산을 검증하기 위해 만든 합성 데이터입니다.

## Columns

- `date`
- `policy_rate`
- `inflation_yoy`
- `unemployment_rate`
- `term_spread`
- `credit_spread`

## Usage

```bash
python -m src.run_baseline
```

